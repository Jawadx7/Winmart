import { item } from "../../Data/models"
import './Hero.scss'

interface Props{
    item: item
    width: string
}

const CarouselItem = ({item, width}: Props) => {
  return (
    <div>hello</div>
  )
}

export default CarouselItem
